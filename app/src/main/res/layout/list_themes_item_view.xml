<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/themeColor1"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/kp_margin"
    android:padding="@dimen/kp_margin"
    android:background="@color/kp_wb_light_white">

    <ImageButton
        android:id="@+id/themeColor2"
        android:layout_width="@dimen/kp_min_image_height"
        android:layout_height="@dimen/kp_min_image_width"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:background="@color/kp_wb_blue"
        android:fontFamily="@font/spacemono_regular"
        android:contentDescription="@string/themes"
        android:gravity="center_vertical">

    </ImageButton>
</RelativeLayout>